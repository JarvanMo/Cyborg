
//apply from :  rootProject.getRootDir().getAbsolutePath()
if(runAsApp.toBoolean()){
    apply from : "${rootProject.getRootDir().getAbsolutePath()}/scripts/module_application.gradle"
}else {
    apply from :  "${rootProject.getRootDir().getAbsolutePath()}/scripts/module_library.gradle"
}
